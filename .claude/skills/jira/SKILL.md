---
name: jira
description: Manages Jira tickets via the local CLI
allowed-tools: [Bash]
---
# Task
Use the `acli` (or `jira`) command to fetch, update, or comment on tickets.
Always return the output in JSON format if possible so you can parse it accurately.