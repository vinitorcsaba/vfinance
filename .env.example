# Database (SQLite path — relative to project root)
DATABASE_URL=sqlite:///data/vfinance.db

# Google Sheets export (optional — leave blank to disable)
GOOGLE_SHEETS_KEY_PATH=
GOOGLE_SHEETS_SPREADSHEET_ID=
